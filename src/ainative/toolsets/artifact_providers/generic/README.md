# Generic Providers

Future image, video, mesh, audio, or model services implement the `ArtifactProvider` seam. Provider-specific queue and transport details stay behind that seam and are loaded only by the selected Artifact Workflow.
