# Project Toolset Ports

These protocols define interfaces concrete project Toolsets implement. They are
shared by the Project Tool Registry and Toolset packages; they do not contain
Workflow policy, MCP protocol handling, or host-specific implementation.

MCP connections are owned separately by external Agent MCP configuration. MCP Tools are not
registered into the Project Tool Registry.
