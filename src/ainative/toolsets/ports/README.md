# Project Toolset Ports

Protocols that define interfaces concrete project Toolsets implement. Shared by the Project Tool Registry and Toolset packages; they do not contain Workflow policy, MCP protocol handling, or host-specific implementation.

MCP connections are owned separately by external Agent MCP configuration. MCP Tools are not registered into the Project Tool Registry.
