# UE5 Fixtures

```text
ue5/
├── actor-fixture/       generic UE5 Actor host-operation project
└── assetsbridge-smoke/  UE5 AssetsBridge round-trip project
```

Keep `.uproject`, `Config/`, `Content/`, `Plugins/`, and fixture source here.
`DerivedDataCache/`, `Intermediate/`, `Saved/`, and similar folders are
generated editor state.
