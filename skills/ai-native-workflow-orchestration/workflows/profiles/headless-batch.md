# Profile: Headless Batch

Use the headless-batch profile for repeatable background execution, explicit
file inputs, and machine-readable evidence.

Headless-batch does not change preservation requirements. It only changes the
allowed interaction and process policy.
