# Profile: Default

Use the default profile when the task does not require a special interaction or
batch policy.

The profile leaves Tool selection to the Workflow and does not imply a
specific Backend or Call Surface.
