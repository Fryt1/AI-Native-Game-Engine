docs: finish the Plan -> Workflow rename in prose and config

The identifier rename landed in the previous commit; the surrounding prose and
two config files still called the artifact a "plan", so a reader saw both names.

- 35 prose occurrences across docs/, guidance/, references/, and src/ docstrings
  now say Workflow.
- .gitignore: dropped four `vendor/**` rules; vendor/ was deleted.
- pyproject.toml: "Agent-authored plans" -> "Agent-authored Workflows".
- templates/workflow-plan-template.md: it and workflow-template.md both opened
  with "# Workflow Template", so the two were indistinguishable at a glance. The
  plan template is now "# Workflow Plan Template".

Left alone deliberately:
- `state.py` reads the old "plan" state key, and `deserialize.py` names the old
  wrapper shape in an error path: both are backward compatibility.
- test fixture ids such as "plan-lifecycle" and the template's own file name.
- README.md "an AI agent can plan host edits" and every re-plan/replan: the verb.

95 tests pass; integrity gate ok; ruff clean.
